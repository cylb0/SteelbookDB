spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/steelbook_db_test
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    database: POSTGRESQL
    show-sql: true